generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Filme {
  id     Int    @id @default(autoincrement())
  nome   String
  genero String
}

model Usuario {
  id    Int    @id @default(autoincrement())
  email String @unique
  senha String
}